# Frontend mock backend switch
# Leave unset for the default behavior:
# - `pnpm run dev` uses the mock backend in a plain browser
# - `pnpm tauri dev` uses the real Tauri backend

VITE_ENABLE_MOCK_BACKEND=true
